interface:
  display_name: "Read Discord Conversation"
  short_description: "Read one exact Discord conversation"
  default_prompt: "Use $read-discord-conversation to retrieve and present this exact Discord conversation with explicit gaps."
dependencies:
  tools:
    - type: "mcp"
      value: "discord"
      description: "Retrieve Discord messages and threads through an authorized bot"
