interface:
  display_name: "Read Slack Thread"
  short_description: "Read a Slack thread and supported files"
  default_prompt: "Use $read-slack-thread to read this thread, every reply, and selected supported files."
dependencies:
  tools:
    - type: "mcp"
      value: "slack"
      description: "Read Slack messages and threads"
