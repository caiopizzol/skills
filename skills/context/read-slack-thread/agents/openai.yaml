interface:
  display_name: "Read Slack Thread"
  short_description: "Read one exact Slack thread"
  default_prompt: "Use $read-slack-thread to retrieve and present this exact Slack thread with explicit gaps."
dependencies:
  tools:
    - type: "mcp"
      value: "slack"
      description: "Retrieve Slack messages and threads"
