interface:
  display_name: "Read Linear Issue"
  short_description: "Read a Linear issue and resolvable linked context"
  default_prompt: "Use $read-linear-issue to retrieve this exact Linear issue, its owned context, and exact external references available through configured read-only provider tools, with explicit source boundaries and gaps."
