interface:
  display_name: "Read Linear Issue"
  short_description: "Read one Linear issue and its owned context"
  default_prompt: "Use $read-linear-issue to retrieve and understand this exact Linear issue, its files, and every Linear-owned context lane with explicit gaps."
