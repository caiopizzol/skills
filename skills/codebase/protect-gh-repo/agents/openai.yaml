interface:
  display_name: "Protect GitHub repository"
  short_description: "Protect merges with checks already seen"
  default_prompt: "Use $protect-gh-repo to check and protect this repository's default branch."
