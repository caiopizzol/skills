interface:
  display_name: "Set up GitHub checks"
  short_description: "Run local checks in GitHub Actions"
  default_prompt: "Use $setup-gh-checks to run this repository's existing local check in GitHub Actions."
