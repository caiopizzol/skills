interface:
  display_name: "Create GitHub repository"
  short_description: "Check, create, or connect a GitHub repository"
  default_prompt: "Use $create-gh-repo to check, create, or connect this project's GitHub repository."
