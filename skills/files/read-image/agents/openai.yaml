interface:
  display_name: "Read Image"
  short_description: "Read one local image"
  default_prompt: "Use $read-image to inspect this image and report its format, findings, frame coverage, and gaps."
