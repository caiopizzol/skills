interface:
  display_name: "Resolve PR Thread"
  short_description: "Reply, react, and resolve one PR thread"
  default_prompt: "Use $resolve-pr-thread to close this checked pull request review thread."
